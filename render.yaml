services:
  - type: web
    name: heatloss-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn predict:app --host 0.0.0.0 --port $PORT
    plan: standard